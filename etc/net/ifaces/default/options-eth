# Optional now.
#MODULE=

# LINKDETECT option allows skip DHCP/ZCIP timeout, if ethernet cable is
# not plugged in during initialization. This feature requires ifplugstatus
# and is off by default, because not all network cards report correct link
# status.
#LINKDETECT={yes|no}
LINKDETECT=no
